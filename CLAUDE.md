# Working preferences

- Always start in plan mode before making any code changes, running non-readonly commands, or committing — even if the request seems small. Wait for explicit plan approval before executing.
